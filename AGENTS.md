# Syntax — контекст для AI-разработчика

Фронтенд обучающей платформы «Syntax» (учим программирование в браузере).
Работа ведётся в этом репо; бэкенд пока не существует — все данные статические (в конфигах/i18n).

## Запуск
- `npm run dev` → http://localhost:5173 (лог: `/tmp/vite-dev.log`). Vite обязан работать с `server.watch.usePolling: true` — НЕ убирать (AI меняет иноды файлов, watcher виснет без поллинга).
- `npm run build`, `npm run lint` — обязаны быть зелёными после каждого изменения.
- Версии: Vite 7.3.6, React 19.2.8, @vitejs/plugin-react 5.2.0, @monaco-editor/react 4.7.0, emmet-monaco-es, flag-icons.

## Архитектура
- `src/App.jsx` — hash-роутинг (`#/roadmap`, `#/editor`, …) + «job»-состояние: `openTab(tab, job)` передаёт контекст урока/задачи в редактор. Home = `activeTab === "home"` (в сайдбаре нет, по умолчанию при загрузке).
- `src/components/MainContent.jsx` — свитч вкладок: `home → MainView`, `roadmap → RoadmapView`, `tasks → TasksView`, `rankings → RankingsView`, `documentation → DocsView`, `community → CommunityView`, `editor → CodeEditor`, остальное → заглушка lesson-карты.
- **Правый сайдбар унифицирован**: `WidgetPanel` получает `activeTab` и `onNavigate`; таблица `TAB_ASIDES` мапит вкладки → `src/components/panels/{RankingsAside, CommunityAside, DocsAside, TasksAside}.jsx`. Вьюха НЕ содержит свой правый rail — она одноколоночная, aside монтируется во внешний rail. Две колонки справа — баг.
- **Хедер универсальный** (`Header.jsx`): логотип (→ home), флаг-язык, тема, уведомления, **гостевой блок auth** (Log in ghost + Sign up free primary, класс `.auth`; на ≤1100px только пилюля, ≤480 скрыт колокольчик `.tb-menu-wrap--notif`), аватар. Таб-специфичный контент (заголовки, поиска, сегмент-контролы) живёт ВНУТРИ вьюх. `.topbar` — `sticky; z-index: 50`; НИКАКИХ поздних правил, перебивающих его position/z-index (был баг: `.topbar, .shell { position: relative; z-index: 1 }` — топбар ехал под липкие карточки сайдбара).
- **SignupModal** (`src/components/SignupModal.jsx`, state в App: `openSignup`/`closeSignup`): одна модалка на ВСЕ гостевые действия (Start for free, Continue Learning, Start Challenge, финальный CTA, кнопки хедера). Пока без бэкенда: submit просто закрывает; «Continue as guest» — тоже. Esc/клик-аутсайд, body-scroll-lock.
- Дропдауны хедера: `hidden` + глобальное CSS `[hidden] { display: none !important }` (классы с `display:flex` иначе перебивают UA-правило). Клик-аутсайд — на `click`, каждый дропдаун закрывается независимо.
- `CodeEditor.jsx` — Monaco (загружается с CDN **0.55.1** через @monaco-editor/loader — не путать с node_modules). Emmet НЕ в ядре Monaco 0.5x: подключается плагином `emmet-monaco-es` в `beforeMount` (`emmetHTML(emmetCSS(emmetJSX))`, гвард `monaco.__syntaxEmmet`). Рабочее пространство файлов: старт-файл с кодом, новые файлы пустые, дубликаты имени → `(2)`, `(3)`, табы перетаскиваются и закрываются. Run: JS-only файлы — скрытый раннер-iframe, HTML — перезапуск превью; не-JS (py/sql/css) — честная подсказка «runner coming soon» в консоли (Run не молчит). `console.log/warn/error` и ошибки перехватываются скриптом `CONSOLE_CAPTURE` в iframe; **номера строк ошибок — в координатах файла**: перед каждым JS-блоком отдельный тег `window.__syntaxOffset = N` (обязателен отдельным `<script>`: при SyntaxError сам блок не исполняется); клик по ошибке → revealLineInCenter. Консоль всегда в кадре (подсказка до первого Run, авто-скролл + вспышка после). Submit = run + вердикт.

- **AI Assistant (Gemini, 2026-09 переезд с HuggingFace/gemma — HF-кредиты сгорели, 402)**: модель **`gemini-flash-lite-latest`** (2026-09-02 перебрано: 3.6-flash — thinking-модель, 10с до первого токена, free-tier всего 20 req/день — убийственно; `thinkingLevel: "low"` режет до ~2с, но квота та же; flash-lite — ~0.8–1.5с, не «думает», free-tier в разы больше, качество на tutor-вопросах проверено ок; gemini-2.5-flash для новых юзеров 404). `src/lib/ai.js` — `POST https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:streamGenerateContent?alt=sse`, header `x-goog-api-key`, SSE-парсинг: текст в `candidates[0].content.parts[].text`, история → `contents` (user/model), сис-промпт → `systemInstruction`, `generationConfig {temperature: 0.4, maxOutputTokens: 2048}`. Ошибки Gemini: `API key not valid` (400) → badKey, 429/RESOURCE_EXHAUSTED → rateLimit, 402 → credits, SAFETY-finish → http. **Ключ — двухрежимный (как и было с HF, та же схема после инцидента с токеном)**: DEV — `VITE_GEMINI_API_KEY` в `.env.local` → прямой запрос к Google; PROD — без ключа → **`api/ai.mjs` (Vercel serverless-прокси, SSE passthrough)**, ключ в Vercel env **`GEMINI_API_KEY`** (Production, БЕЗ VITE_-префикса). `isAiConfigured()` = dev-ключ || PROD. `src/lib/ai-prompt.js` — сис-промпт (общий для клиента и прокси; чистая функция без import.meta; **дублируется в `api/ai.mjs` — кросс-папный импорт из ../src ломает Vercel-функцию, при правке синхронизировать обе копии**; содержит блок «Platform facts» — 9 треков = зеркало TECHS (изменения TECHS → обновить TRACKS вручную), курс HTML = 16 уроков, docs = Python, «не выдумывай треки/курсы, не знаешь — скажи» — без него модель приписывала несуществующие курсы). `askAssistant({techName, history, question, onToken, signal})` — история 10; `AiError.code`: noKey/badKey/rateLimit/modelLoading/network/http → i18n `ai.err*`. `src/components/AiChat.jsx` — чат в rail-карточке: заголовок сверху, **имя модели под заголовком** (chip), кнопка очистки вверху чата, приветствие «Нужна помощь в обучении?» — **первым AI-пузырём в чате**, плейсхолдер «Спроси что угодно.»; чат — фиксированная 320px вертикальная скролл-зона (`overflow-x: hidden`; пузыри `flex: none` — иначе md-пузырь (overflow) сжимается flex-ом и контент обрезается); автоскролл «прилипает» к дну, пока пользователь рядом (stickRef, onScroll) — вверх скроллит свободно даже при стриминге. `fixMd()` — модель пишет fence в строке с кодом и обрывается на max_tokens с незакрытым блоком (нечётное число ``` → доклеить). i18n: `ai.*` + `techPage.aiGreet/aiInput` ×5. **После изменения `.env*` dev-сервер перезапустить.**
- **Supabase (первые данные из БД)**: `src/lib/supabase.js` — клиент + `fetchDbLessons()` (таблица `lessons`: id/title/content/**code**/tech; таймаут 4с, кэш на сессию; сбой/пусто → null/[] → fallback на i18n). App тянет lessons при mount → MainContent: урок трека = первая строка `lessons` с `tech = id трека` (title + content; file по треку) с флагом `job.fromDb` → зелёный чип «From database» (`editor.dbSource`, 5 языков); CodeEditor подставляет `job.code` в старт-файл (и в Reset). **Строка в `lessons` = `tech` (id трека), `title`, `content`, `code`, id — uuid; порядок уроков = порядок id** (TechnologyView нумерует с 1, «первый» урок открывает Continue). `content` — markdown-lite (контракт в `src/lib/markdown.js`, рендер `src/lib/markdown-view.jsx`): `##`/`###`, `**жирный**`, `*курсив*`, `` `код` ``, блоки ```lang, `TIP:`/`NOTE:`/`WARN:` → callout'ы; **СПИСКОВ И ТАБЛИЦ НЕТ** (строки в одном параграфе схлопываются — каждый пункт = отдельный параграф через пустую строку). TechnologyView: секция «Lessons» (`techPage.dbLessons/dbLessonOpen`, 5 языков) — уроки трека из БД, нумерация с 1, клик по строке (`.techmod--db`) → вкладка **`#/lesson` → LessonView**: шапка (chip тех · Lesson, From database, Back→свой трек) + 2 колонки: **материал** (markdown-lite) | **редактор** (тот же CodeEditor, свой job-заголовок скрыт CSS `.lesson-view .editor-job{display:none}`); мобилка — в столбец. **Контракт контента урока (для агента-наполнителя)**: `lessons { title, tech, content, code }`, где `content` = markdown-lite: `##`/`###`, `**жирный**`, `*курсив*`, `` `код` ``, ```lang-блоки (Copy-кнопка), `TIP:`/`NOTE:`/`WARN:` → callout'ы; `code` = стартовый код задания в Monaco. Парсер: `src/lib/markdown.js` (чистый, без JSX: parseMdBlocks/inlineMdTokens) + рендер `src/lib/markdown-view.jsx` (MdContent; разделение — react-refresh правило «только компоненты»). i18n: `lessonView.{material,task,noMaterial}` ×5. Статичные i18n-уроки (без строк в БД) — как раньше, в редакторе. Ключи: `.env` в репо (`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`; publishable — безопасно коммитить, Vercel-сборка работает без env-панели). База `xaslezkoktydranikqnx.supabase.co`, таблицы profiles/lessons/courses/modules (кроме lessons — пусты). **RLS-лента**: anon нужны политики SELECT/INSERT/DELETE `USING/WITH CHECK (true)` + GRANT; **UPDATE-политики НЕТ** (403) — правка строки = DELETE + INSERT (или создать политику). Ловушки: строка в БД + SELECT-политика не `USING (true)` → API молча `[]` (лечится новой политикой — пермиссивные суммируются по OR); DELETE через API без DELETE-политики → молча 204, строка не удаляется.

- **Auth (Supabase, 2026-09)**: `src/lib/auth.js` — signInWithPassword/signUp/signOut + `readStoredSession()` (синхронно из localStorage `sb-<ref>-auth-token` → без гостевого мигания на load; асинхронно getSession + onAuthStateChange подтверждают). Ловушка: `onAuthStateChange` возвращает `{data:{subscription}}` — unsub через `subscription.unsubscribe()`. После SIGNED_IN — `syncProfile(user)` fire-and-forget upsert в `profiles` (колонки: **id, email, full_name, avatarurl** — без подчёркиваний; RLS-политики для authenticated могут не быть — имя показывается из user_metadata, сбой не критичен). App: `session` state, `isAuthed=Boolean(session)`, `userName` (displayName: full_name → префикс email), модалка `authMode: null|"signup"|"login"` — **условный рендер + key=mode** (remount = свежий state; sync-reset в effect ломает react-hooks/set-state-in-effect). `AuthModal.jsx` (SignupModal удалён): signup = имя+email+пароль, login = email+пароль, busy-спиннер, ошибки `auth.err*` (invalid/unconfirmed/exists/weak/email/rateLimit/network/generic — маппинг GoTrue-сообщений в `mapAuthError`), **signUp без session → панель «Check your inbox»** (Supabase требует подтвердить email). Header: `user ? меню (монограмма-инициал, hue от имени, name/email, Профиль[soon], Settings, Log out) : .auth-кнопки (Log in → login-режим, Sign up free → signup)`. **LOVUHA: Supabase «Confirm email» (dashboard: Authentication → Sign In/Providers → Email) должен быть ВЫКЛЮЧЕН** — иначе signUp шлёт письмо и 429 `over_email_send_rate_limit` (проектный лимит; не доменный, не IP — лечится только выключением подтверждения или ожиданием окна). Пароль ≥ 6 (валидация в модалке до запроса). i18n: `auth.{loginTitle,loginSubtitle,password,loginSubmit,toLogin,toSignup,loginCta,signupCta,emailSent*,err*}` ×5. Демо-auth из SignupModal (in-memory isAuthed) удалён: теперь Sidebar-прогресс/SAMPLE-чип реагируют на реальную сессию.
- **Курс HTML в БД (16 уроков)**: исходники — `course/html/` (`00-roadmap.md` с контрактом + `01..16-*.md` + `code/01..16.html`). Порядок: структура → кодировки → текст → ссылки → навигация/вкладки → изображения → списки → таблицы → формы×2 → семантика → медиа → a11y → мета/SEO → современные элементы (dialog/details/time) → финальный проект. **Контракт урока** (фиксирован, QC в сидере): 5 разделов (Цель/Теория/Пример/Частые ошибки/Практическое задание), минимум 1 `TIP:` и 1 `WARN:` (ошибки = WARN-пункты), без таблиц/`[ссылок](url)`, объём content 4000–7000 зн. **`code` = скелет ЗАДАНИЯ с `<!-- TODO -->` (не решение!)** — лежит в `code/NN.html`. Сидер `seed-html-course.mjs` (корень репо): `node seed-html-course.mjs` — IDEMPOTENT (удаляет ВСЕ tech='html' и вставляет 16, id `10000000-…00NN`), встроенный QC (5 разделов, callout'ы, ссылки, объём, html-блок) — падает до БД при нарушении; `DRY=1 node …` — только проверка без записи. Ловушка: в markdown-lite нет списков/таблиц (буллеты → «• » строки через пустые строки), callout-строка — plain text (без ** и ` внутри).
- **Курс JavaScript в БД (42 урока)**: исходники — `course/js/` (`00-roadmap.md` + `01..42-*.md` + `code/01..42.js`). Логика: переменные/типы (01–07) → функции/замыкания/колбэки (08–15) → массивы (16–21) → объекты (22–27) → ошибки/отладка (28–29) → DOM (30–35) → storage/таймеры (36–37) → event loop/Promise/async/await/fetch (38–41) → финальный проект (42). Тот же контракт, что HTML (5 разделов, TIP+WARN, 4000–7000 зн, без таблиц/ссылок). Отличия: `code` = `index.js` — **выполнимый сразу** скелет с `// TODO` (раннер = обычный `<script>`, **НЕТ top-level await** — асинхронка через IIFE/`.then`); DOM-уроки (30–35, 42) содержат в материале ```html-каркас (студент создаёт index.html рядом — превью живое, JS инжектится автоматически). Сидер `seed-js-course.mjs` (корень): IDEMPOTENT (удаляет ВСЕ tech='javascript', вставляет 42, id `30000000-…00NN`), QC (5 разделов, TIP/WARN, ```js, объём, ссылки) + `DRY=1`.
- **Курс React в БД (18 уроков)**: исходники — `course/react/` (`00-roadmap.md` + `01..18-*.md` + `code/01..18.jsx`). Логика: React/JSX/компоненты/пропсы (01–04) → условия/useState/события/списки+key (05–08) → useEffect: cleanup, данные, ловушки (цикл/deps/stale closure), useMemo/useCallback (09–12) → правила хуков/кастомные хуки, useFetch, useRef, Context (13–16) → асинхронные паттерны (debounce/abort), финальный проект «Заметки» CRUD (17–18). Тот же контракт (5 разделов, TIP+WARN, 4000–7000 зн, без таблиц/ссылок), но QC требует ```jsx-блок. `code` = `App.jsx` — **исполняемый сразу** скелет с `// TODO`; компонент называется **App** (раннер его маунтит; поддерживаются `function App`, `const App`, `export default`). Сидер `seed-react-course.mjs` (корень): IDEMPOTENT (удаляет ВСЕ tech='react', вставляет 18, id `40000000-…00NN`), QC (5 разделов, TIP/WARN, ```jsx, объём, ссылки) + `DRY=1`.
- **React-раннер (CodeEditor, 2026-09)**: `.jsx`-файл в воркспейсе → `buildReactDoc()` вместо обычного раннера: код студента в `<script type="text/plain">` → **Babel standalone** (unpkg, `Babel.transform` с `runtime: "automatic"` — НЕ data-presets, он JSON не парсит) → **blob-module** → динамический `import()` → `createRoot(...).render(App)`. React 18.3.1 — **esm.sh через import map** (`react`, `react/jsx-runtime`, `react-dom/client` — одна копия и для импортов студента, и для раннера). Авто-маунт: инлайн-вызов `__SyntaxMount(typeof App…)` + фолбэк `mod.default`, флаг `window.__syntaxMounted` от двойного маунта. Номера строк: маркер-комментарий `// __SYNTAX_FILE_START__` — его строка в ТРАНСФОРМИРОВАННОМ коде = `window.__syntaxOffset` (e.lineno у blob-module — в координатах blob, а не документа!). `__SyntaxMount` — ОБЯЗАТЕЛЬНО `window.` (function-decl в модуле = модульная область). Монaco: `.jsx` → язык `"jsx"` (emmetJSX регистрируется и на jsx/tsx); `buildJsBlocks` исключает .jsx из сырых `<script>` (иначе SyntaxError в HTML-превью). Маунт ~4с (CDN). Lint: `course/**` в eslint globalIgnores.
- **AI-промпт: опубликованные курсы** = HTML (16) + CSS (22) + **JavaScript (42)** + **React (18)** — строка Courses в `buildSystemPrompt()` (**в обеих копиях**: `src/lib/ai-prompt.js` + дубль `api/ai.mjs`); при каждом новом курсе обновлять обе.
- **Курс CSS в БД (22 урока)**: исходники — `course/css/` (`00-roadmap.md` + `01..22-*.md` + `code/01..22.css`). Цепочка: синтаксис/селекторы → каскад/@layer/специфичность → единицы → box model/display/типографика/цвет → position → Flexbox×2 → Grid×2 → subgrid → media queries (mobile-first) → container queries → clamp/fluid → переменные/темы → transition/@keyframes → финальный проект (адаптивный каталог). **Формат задания специфичен для CSS-трека**: файл редактора = `styles.css` (скелет задания с `/* TODO */`), HTML-каркас — в материале урока (```html блок в «Примере»); студент создаёт index.html рядом — превью CodeEditor АВТОМАТИЧЕСКИ инжектит CSS-вкладку в HTML-файл (buildPreviewDoc), `<link>` не нужен. Сидер `seed-css-course.mjs` — как HTML-сидер, но tech='css', id `20000000-…`, QC требует ```html И ```css блоки. **Ловушка markdown-lite (попалась при наполнении)**: буллет/нумерованный пункт, СЛЕДУЮЩИЙ ЗА строкОй абзаца БЕЗ пустой строки, схлопывается в абзац (первый пункт «прилипает») — в исходниках ПУСТАЯ СТРОКА перед каждым списком обязательна (фикс-скрипт в хронике 2026-09 «курс CSS»).
## Конвенции
- Дизайн-система «Protocol Neo»: токены тем в `src/css/style.css` (`:root` тёмная, `[data-theme="light"]`). Скроллбары: тонкие, тёмная тема — акцентный зелёный, светлая — нейтральный (токены `--scroll-thumb*`).
- i18n: **5 языков** EN/RU/UK/ES/DE. `src/i18n/index.js` (EN + RU), `locales-extra.js` (UK/ES/DE). `useT()` — dot-lookup, `{param}`-интерполяция, fallback на EN. Список-данные (посты, модули, задачи, гиды, уведомления) — переводятся как массивы в словарях; state в компонентах держит **индексы**, не строки. Новый ключ обязан попасть во ВСЕ 5 словарей.
- Выбор языка: `localStorage["syntax-ui-lang"]`, тема: `localStorage["syntax-theme"]`, синхронизируется `<html lang>`.
- Аватары: монограммы-кружки с HSL-градиентом (`src/components/Avatar.jsx`), фото придут с бэкендом.
- i18n-аудит ключей (обязателен после добавления t("...")): `npx esbuild src/i18n/index.js --bundle --format=cjs --outfile=/tmp/locales.cjs` + node-скрипт: собрать все `t("key")` из jsx, сверить с LOCALES по всем 5 языкам (шаблон скрипта — в хронике/прошлых сессиях; node ESM напрямую не импортирует i18n — там extension-less import, только через esbuild).

## Дизайн-макеты (эталон)
`/mnt/c/Users/User/Desktop/SyntaxAddDesign/`:
`Syntax-main_dashboard`, `Syntax_roadmap_dashboard`, `Syntax-tasks_dashboard`, `Syntax-documentation_dashboard`, `Syntax-rankings_dashboard`, `Syntax-comunity_dashboard` (опечатка в имени — один m). В каждом: `screen.png` + `code.html`.

## Состояние (на момент правки этого файла)
Сделано: каркас, все вкладки, i18n 5 языков, hash-роутинг, замкнутый учебный цикл, унифицированный правый сайдбар, эммет, live-превью, мобилка, a11y. **Supabase-урок** (коммит 78d1e18): редактор показывает урок из таблицы lessons (первая строка) с чипом «From database», fallback на i18n при пустой/мёртвой БД.
**Технологии и треки (UX-аудиты раунды 1–3)**: страницы технологии `#/technology/<id>` (hero лого+описание+Open track+N lessons, курс-прогресс, curriculum; контент `techs.{id}` — EN+RU, UI-ключи `techPage.*` 5 языков; rail: Resources + AI Assistant). Выбранный трек: `localStorage["syntax-tech"]`, deep-link (трехк в hash, валидация против TECHS), «Not your track?» → каталог. **Лого в хедере ВСЕГДА оригинальный Syntax → главная** (фича «лого меняется на трек» убрана по фидбеку — лого трека уже на самой странице). Roadmap = карта РЕАЛЬНОГО курса активного трека (переделан 2026-09, см. хронику): уроки из таблицы `lessons` (tech = трек, порядок = id), прогресс — localStorage (`src/lib/progress.js`: отметки ставит успешный Submit в CodeEditor, key `syntax-progress-<tech>`); статус «Урок X из N · a из b · %», стр done кликабельны (повторить), current — доминирующий, locked после «Up next»; CTA Continue = первый невыполненный (`openLesson`). Трек без уроков — empty-state (docs-empty-стиль) + CTA на HTML-курс; БД грузится — скелетон. `openDbLesson(lesson, techId, backTab)`: с roadmap backTab="roadmap". Демо-модули i18n `techs.{id}.modules` в roadmap не используются (остаются в TechnologyView progressModule). Переключатель треков — пилюли с лого. **В БД на 2026-09: 39 уроков — html 16, css 22, javascript 1 (тестовый); `course/js/` — новый курс JS пишется параллельно** (eslint: `course/**` в globalIgnores — скелеты заданий со TODO не линтаются). Порядок `TECHS` (lib/techs.js) = логика «стек снизу вверх»: html, css, javascript, python, react, vue, node, mongo, postgres — задаёт ВСЕ сетки (главная, roadmap-switcher, табы tasks). Урок/задача = файл трека: `techs.{id}.lesson { title, desc, file }`, CodeEditor/taskJob берут файл и Monaco-язык по расширению (main.py / queries.sql / index.html / styles.css / index.js).
**Tasks-раздел знает о треке**: 12 задач (3 general + JS 2, Python 2, HTML 2, CSS 2, PostgreSQL 1), tech-табы (General В КОНЦЕ списка), заголовок «{Track} Tasks», чипы категорий, счётчик «N of M · track», tech-empty («No {tech} tasks yet» + CTA), search-empty с «Clear search», карточка кликабельна целиком, «All Statuses» честно деактивен (soon-бейдж), Daily Challenge — чип «General», rail-аналитика гостю — чип «SAMPLE» (isAuthed проброшен App→WidgetPanel→Aside).
**Редактор (UX-аудит)**: консоль ВСЕГДА в кадре (до Run — подсказка; после Run — авто-скролл + вспышка; после failed-Submit — скролл к центру). Run не молчит для не-JS: «runner coming soon» (py/sql) / css-only-hint; Submit без раннера — без ложного accepted. Номера строк ошибок в координатах файла: тег-маркер `window.__syntaxOffset = N` в ОТДЕЛЬНОМ `<script>` ПЕРЕД каждым JS-блоком (при SyntaxError сам блок не исполняется — маркер обязателен отдельным тегом!), `fixJsOffsets()` в CodeEditor; клик по ошибке → revealLineInCenter. Ctrl/⌘+Enter = Run (addCommand, runRef через useEffect — react-compiler не любит присваивание ref в рендере). Reset кода с инлайн-подтверждением (initialContentsRef, авто-cancel 5с). Drag-сепаратор редактор/превью (25–75%). Бесконтекстный редактор = песочница (строка free-mode + ссылка на Tasks). Back из урока ведёт на СВОЙ трек (job.techId → onNavigate(backTab, {techId})); чип урока с треком «Python · Lesson». Мобилка: «Submit solution» → «Submit» (btn-label--full/--short).
**Rankings (UX-аудит)**: ступенчатый подиум 2-1-3 (высота = ранг, трофей над №1, стрик + дельта); XP-ядро строки (3-я колонка, моно), дельты ▲/▼ в Rank; ранги таблицы = i+4 (подиум занимает 1–3; согласовано с рейлом «#6», «280 XP to #5»); you-строка: scrollIntoView при mount/смене фильтров + прогресс-бар «до #5» (97% = 9840/10120, совпадает с рейлом). Динамический заголовок {scope} Rankings, скоупы с разными данными (SCOPES в RankingsView), периоды Week/Month/All-time (PERIOD_MULT), tech-фильтр (TECH_FACTOR) — демо-множители, пересортировка; tbody key от фильтров (fade-анимация), ключи строк — по имени (FLIP-ready). View All — span с soon-бейджем (не мёртвая кнопка). How-ranking popover (источники XP + правила сброса). Лига: заметки порогов + маркер «You». Weekly: дни недели (rankings.weeklyDays — массив в 5 языках), tooltip «+N XP», «+{total} XP this week». Мобилка: table-layout fixed, Level/Tasks скрыты (col-level/col-tasks), уровень под именем (rankings__user-sub). **Bottom-nav: 8 пунктов (6 основных + Settings/Support) — flex 6:2 на двух .nav-блоках + эллипсис, НИЧЕГО не обрезается.**
**Community Hub (UX-аудит)**: замкнутая петля обсуждения. Клик по посту (карточка целиком, role=button) → **ThreadModal**: дерево ответов (вложенные `reply--nested`, `reply--accepted` с бейджем, код-блоки, чип «Related lesson», автор-клик → профиль), ответ внизу + инлайн «Reply» на каждый топ-ответ. **ComposerModal**: заголовок, markdown-lite (**жирный**, `код`, ``` блоки — MdText/inlineMd), Preview, теги из существующих (max 4), Publish → пост добавляется в фид (localPosts, «Your question», Unanswered+1). Табы Active/Newest/Unanswered — реальные сортировки (activeMin/createdMin) + счётчики; **Unanswered = 0 ответов**. Track-контекст: «My track: {tech}»-пилюля + карточка в рейле «Your {tech} track · N · M new» (клик = фильтр). «Showing X of Y» под табами. Аватар+реп у автора в фиде. Теги в постах — кнопки (тот же CustomEvent syntax-community-tag; **матчинг убирает # из query** — баг пустого фида по трендовым тегам исправлен). Апвот: toggle + tooltip «полезность». Load More убран. Пустой фид: причина + «Reset filters» / «Ask the first question». Рейл: Top Contributors кликабельные (профиль-модалка: реп/посты/стрик/Follow) + Follow-кнопки. Esc/клик-аутсайд; контент ответов — i18n posts[i].replies (EN+RU, uk/es/de копия EN). Мобилка: 2-рядная шапка, CTA = иконка «+» 42px, голосовая колонка уходит вниз (order:2). `avatar-dot--xs` (22px) — новый размер аватара. activeTech === "none" — «нет трека» (нормализация `tech` в CommunityView).
**Documentation (UX-аудит)**: статья стала сущностью. **8 статей Python** (4 Core Reference + 4 Guides) — i18n `docs.articles` [{slug, cat, title, desc, minutes, updated, body:[{type: p|h2|code|callout}]}] (EN+RU, uk/es/de копия EN). **Deep-link `#/documentation/<slug>`**: parseHash → App `routeParam` state → MainContent → DocsView (валидация `py-`-префикс). Трёхколоночный шаблон: левое дерево (2 категории, сворачиваются; sticky; мобилка ≤900 — горизонтальные чипы) | статья | on-page TOC во внешнем рейле (DocsAside). **TOC-канал: CustomEvent `syntax-docs-toc` + request-событие** — при deep-link aside монтируется ПОСЛЕ первого dispatch view (порядок эффектов: view→aside), поэтому aside при mount шлёт `syntax-docs-toc-request`, view ре-диспатчит. Scrollspy: последний h2 с rect.top ≤ 130 (h2 со scroll-margin-top: 84). Селектор «Documentation for:» (тех-пилюли с Soon-бейджами, дефолт = активный трек; **смена трека сбрасывает hash на #/documentation**). Трек без статей → **empty state**: лого, «Docs for {tech} — in development», прогресс «3 of 12», «Notify me» (toggle), «Пока — Python-гайды». Живой поиск: статический индекс по title/desc/body, дропдаун с `<mark>`, счётчиком, «Nothing found for q», Esc; ⌘K-фокус (Header шлёт syntax-focus-docs-search) сохранён. Код: regex-подсветка Python (PY_TOKEN: комментарии/строки/ключевые/числа → tok-*), 13.5px, white-space: pre + overflow-x auto (мобилка — горизонтальный скролл без переноса), Copy-кнопка с toast «Copied ✓». Callouts: tip/note/warning (один компонент, три цвета). Breadcrumbs, «≈ N min read · Updated», prev/next (подписи `docs.prevArticle/nextArticle` — не «← Docs»), «Was this page helpful?». Guide-карты — <a> на deep-link. **Статья = чистый читаемый режим**: hero (H1 + тех-пилюли) и поиск рендерятся ТОЛЬКО на обзоре (`!current`) — на статье их «каша» убрана по фидбеку (смена трека — через крошку «Docs»); tech-селектор в hero — на всю ширину колонки (`.docs-for { width: 100% }`, паттерн tech-switch roadmap/tasks). **i18n: ключи унифицированы `docs.*` во всех 5 словарях** (было documentation.* в RU/UK/ES/DE → 4 языка видели английский контент; поймано esbuild-аудитом).
**React-Compiler лент** (learned the hard way, не повторять): 1) компоненты НЕ объявлять внутри компонента (Node/ModuleIcon/ReplyItem/ThreadModal/ComposerModal — в топ-уровень; `const X = () => {}` в теле = «Cannot create components during render»); 2) `getTech()` в рендере + `.Logo` — «component created during render» → static-мапы на уровне модуля (`TRACK_LOGOS = Object.fromEntries(TECHS.map(...))`); 3) мутация `let`-флагов в map во время рендера — «Cannot reassign» → предвычислять индексы; 4) setState в useEffect для синка с пропом — «cascading renders» → remount через `key={prop}` в родителе (TasksView key=activeTech); 5) присваивание ref.current в рендере — в useEffect.
Осторожно с i18n: скрипты batch-вставки с вложенными циклами ломали файлы (дубли ключей; однажды ES/DE-блоки склеились в UK-объект) — после любых python-правок: `npm run lint` + esbuild-аудит полноты (`node /tmp/audit.js` — шаблон, собирает t("...") из src и сверяет с LOCALES; roadmap.module — известный false positive на комментарий в useT.js).
Открыто:
1. **Контент треков**: переключатель работает, но контент demo — `techs.{id}` только EN+RU (uk/es/de откатываются на EN), уроки = один старт-файл на трек, рейтинги/задачи — демо-множители. Дальше: полные программы, реальные данные, бэкенд прогресса, FLIP-анимации рейтинга. **Урок из БД = всегда первая строка `lessons`** (в схеме нет tech/desc — когда добавят колонки, сделать маппинг урок↔трек; пока desc берётся из i18n по треку).
2. Вкладки **Settings** и **Support** — ждём макеты.
3. News в дефолтном правом сайдбаре — сейчас Daily Challenge + PromoCard (книга, `src/lib/promos.js` + `src/assets/promo-*.png`; NeoStudio подготовлен, ставится позже на другие страницы). **Medium-новости** (`src/lib/medium.js` + `src/lib/translate.js` + `NewsModal.jsx`) уже живут в уведомлениях хедера (см. хронику) — правый rail пока не тронут.
4. **AI-ассистент — ответы шаблонные** (констатировано, решено оставить как есть): gemma-3-12b-it отвечает вежливо-формально, с пересказом фактов. Если захотим живее — варианты: поднять temperature (0.4→0.7), укоротить сис-промпт, модель побольше из каталога (gemma-3-27b-it — медленнее). Контракты промпта (фа́кты платформы, markdown-lite) в `src/lib/ai-prompt.js` + дубль `api/ai.mjs`.
5. **Гостевые фичи лендинга**: mock-редактор в hero (typing CSS-only), SignupModal (in-memory demo-auth, refresh сбрасывает), «Try a demo lesson». Open: «как это работает» 3-плитки (§2в аналитики), canvas-частицы (§5.1в), бэкенд auth.
6. Git: работаем на master, ahead от origin, push — по команде пользователя.

## Правила
- Перед завершением задачи: `npm run lint` + `npm run build` зелёные.
- Не ломать: polling в vite.config, Emmet-гвард, `[hidden]` правило, TAB_ASIDES паттерн, 5-язычную полноту i18n, `sticky/z-50` топбара (см. архитектуру).
- style.css: единый файл ~3000 строк; мобильные оверрайды home — БЛОКОМ В КОНЦЕ home-секции (базовые правила ниже перебивают media-заголовки выше). При массовой вставке CSS — python-заменой между маркерами секций.
- Скриншоты для проверок: puppeteer-core из `/home/alex_creator_web/screenshot-tool/` + chrome-headless-shell из `~/.cache/puppeteer/` (скрипт кладём ВНУТРЬ screenshot-tool для резолва модулей; запуск `node /tmp/...` не работает). Скрипт-шаблон: `/tmp/shot_syntax.js`.
- Коммиты — только по команде пользователя.

## История (хроника)
- 2026-07: скаффолд Vite+React; статичный макет → компоненты; i18n 5 языков; Monaco (темы, табы файлов, превью, Emmet через emmet-monaco-es); вьюхи home/roadmap/tasks/docs/rankings/community; унифицированный правый сайдбар (TAB_ASIDES); hash-роутинг; учебный цикл (job-контекст, Run, консоль в редакторе, Submit); fix HMR (polling); тонкие скроллбары; мобилка и a11y; уведомления (красные метки важных), меню аккаунта, ⌘K. Коммит d576c60 — финальный на сегодня.
- 2026-07 (раунд 2, гостевая главная по аналитике): hero-оффер (H1 + Start for free + demo-урок + social proof), mock-редактор с typing, live-preview-карта, tech-карты со счётчиками уроков, bento (success-широкий, first-project вместо velocity), цитата-кейс, final CTA, футер, SignupModal, auth-кнопки в хедере, PromoCard (книга) в rail, ambient-усиление, sparkline draw-on, мобилка (nav с подписями), fix z-index топбара, fix i18n-дыр (editor.preview, uk header.home). Коммит f0544b9.
- 2026-07 (UX-аудиты раунды 1–2, техники и треки): home-аудит (hero в первом кадре, порядок секций, кнопка одна, sidebar-группы, футер-синхронизация); страницы технологии (макет technology-page) с контентом по 9 трекам; Roadmap → селектор треков (двойная карта убрана); трек в localStorage + deep-link #/technology/<id>; уроки привязаны к треку (файл/заголовок по расширению); fix: refresh-падение на JS-контент (techId fallback на activeTech). Коммит cb2471b.
- 2026-07 (UX-аудит раунд 3): Roadmap переделан обратно в трек прохождения курса (фидбек пользователя: не каталог, а карта трека) — статус «Модуль X из 4 · %», таймлайн с левой осью, переключатель треков-пилюли, CTA. Коммит 19ca0f8.
- 2026-07 (UX-аудиты: Tasks, Code Editor, Rankings — в working tree, не закоммичено): Tasks знает о треке (12 задач, tech-табы с General в конце, tech-empty, search-empty с CTA, карточка кликабельна целиком, SAMPLE-чип аналитике гостю); порядок TECHS «стек снизу вверх» (html→css→js→python→react→vue→node→mongo→postgres); лого-технологии из хедера убрано (всегда Syntax → главная); редактор: консоль всегда в кадре + авто-скролл/вспышка, Run для py/sql/css «coming soon», номера строк ошибок в координатах файла (отдельный тег-маркер __syntaxOffset) + клик → строка, Ctrl/⌘+Enter, Reset с инлайн-подтверждением, drag-сепаратор, free-mode-подсказка, Back → свой трек, мобильный «Submit»; rankings: подиум 2-1-3, XP-ядро + дельты, you-строка в кадре с прогрессом «280 XP to #5», скоупы/периоды/трек-фильтры (демо-множители), how-popover, лига с порогами, weekly с днями, мобилка без горизонтального скролла, bottom-nav 8 пунктов без обрезки.
- 2026-07 (UX-аудит Community — в working tree): замкнутая петля обсуждения — ThreadModal (дерево ответов, accepted, код-блоки, related-lesson, инлайн-reply), ComposerModal (markdown-lite + Preview + теги, Publish → фид), табы со счётчиками (Unanswered = 0 ответов), track-фильтр «My track» + рейл-карточка трека, «Showing X of Y», аватар+реп у автора, теги-кнопки (фикс бага #-префикса), «Your question» + «N new replies», empty-state с 2 действиями, Load More убран, профиль контрибьютора + Follow, мобилка (2-рядная шапка, CTA-иконка, голосовая вниз), avatar-dot--xs.
- 2026-07 (UX-аудит Documentation, коммит 046a352): статьи как сущность — 8 статей Python (i18n docs.articles, EN+RU), deep-link #/documentation/<slug> (routeParam: App→MainContent), трёхколоночный шаблон (дерево | статья | on-page TOC со scrollspy; TOC через CustomEvent + request-фикс порядка mount), селектор «Documentation for» + empty-state по трекам без статей, живой поиск с подсветкой и счётчиком, regex-подсветка Python + Copy + горизонтальный скролл кода на мобилке, callouts tip/note/warning, breadcrumbs, prev/next, «Was this page helpful?», guide-карты — реальные ссылки, **i18n-унификация docs.* (был разрыв documentation.* в 4 языках)**.
- 2026-07 (докс-полировка + порядок трейков — в working tree): статья — чистый читаемый режим (hero/поиск только на обзоре), tech-селектор на всю ширину, prev/next-подписи, Python переставлен после MongoDB в TECHS (все страницы).
- 2026-09 (Supabase: урок из БД, коммит 78d1e18): подключение Supabase — `@supabase/supabase-js`, `src/lib/supabase.js` (fetchDbLessons: таймаут/кэш/fallback), App fetch при mount, MainContent-инъекция DB-урока в job (title+content, `fromDb`), CodeEditor: `job.code` в Monaco + чип «From database» (i18n ×5). Отладка «тестовый урок не выводится»: (а) таблицы пустые, (б) фронт не был подключён, (в) главная ловушка — RLS: строка в БД, но anon-SELECT-политика её скрывает (API `[]`; лечится `CREATE POLICY ... FOR SELECT TO anon USING (true)`), INSERT/DELETE через API молча «успешны» без политик. Контент `lessons`: **полный курс HTML — 16 уроков** (агент, 2026-09: цель/теория/пример/ошибки-как-WARN/задание с TODO, TIP+WARN callout'ы, code = скелет с TODO; формат-контракт передаётся агенту) + 1 тестовый JS-урок. .env с publishable-ключами коммитится — Vercel без env-панели.
- 2026-09 (уроки из БД по трекам, коммит cf4f541): колонка `tech` в `lessons` (ALTER + UPDATE в SQL Editor — PostgREST-кэш схемы может задержаться на секунды), TechnologyView показывает секцию «Lessons» с уроками трека (первый = первый по id; HTML: «Введение в HTML» — 1-й урок), клик открывает урок в редакторе (title/code из БД, file по треку, чип From database, у HTML-файлов — live-превью контента); Continue на треке = урок СВОЕГО трека из БД; i18n techPage.dbLessons/dbLessonOpen ×5, CSS .techmod--db.
- 2026-09 (курс HTML — 16 уроков, в working tree): курс «HTML5 с нуля» по рекомендациям методиста. `course/html/` — 16 уроков в СТРОГОМ шаблоне (5 разделов: Цель/Теория/Пример/Частые ошибки/Практическое задание; TIP:+WARN:-callout'ы — ошибки оформлены WARN-пунктами; без таблиц/ссылок; 4–7 КБ) + `code/NN.html` — скелеты ЗАДАНИЙ с TODO (не решения). Новые темы: «Кодировки и символы» (урок 2), «Навигация сайта и вкладки» (урок 5), «Мета и SEO-базис» (урок 14; мета-блок из «современных элементов» туда же). Сидер идемпотентен (удаляет все tech='html' → вставляет 16, id `10000000-…`) со встроенным QC (разделы/callout'ы/объём — падает до записи) и DRY-режимом. Проверено скриншотами: 16 уроков на tech-странице, WARNING-каллауты рендерятся, в редакторе — TODO-скелет задания. Нет дублей с CSS/JS (только чистый HTML).- 2026-09 (LessonView: урок-страница из БД — в working tree, не закоммичено): вкладка #/lesson для DB-уроков — материал (markdown-lite из колонки content) + редактор с заданием (колонка code, ALTER выполнен пользователем); demo-урок с полным контентом протестирован через API INSERT/DELETE (callouts TIP/NOTE/WARN, код-блоки с Copy, заголовки) и удалён; контракт формата зафиксирован в AGENTS.md.
- 2026-09 (наполнение БД агентом — в working tree): курс HTML завершён — 16 уроков в `lessons` (audit: все 5 разделов, TIP+WARN, code со скелетом-TODO, дублей нет), E2E на проде: 16 уроков в списке технологии, lesson открывается, callout'ы (1 tip + 5 warn) рендерятся, TODO в коде. JS-тестовый урок без code — шаблонный fallback. Остальные треки ждут наполнения.
- 2026-09 (курс CSS — 22 урока, в working tree): курс «CSS: от синтаксиса к современной вёрстке» по запросу методиста (W3C/MDN, без float-хаков). `course/css/` — 22 урока в шаблоне HTML-курса (5 разделов, TIP+WARN, 4–7 КБ) + `code/NN.css` — скелеты заданий (файл редактора CSS-трека = styles.css; HTML-каркас — в материале, превью подхватывает оба файла без <link>). Сидер `seed-css-course.mjs` (id 20000000-…, QC: ```html+```css). Порядок: intro/селекторы×2/каскад @layer/специфичность+where/единицы → box model/display+collapse/типографика/цвет → position → Flexbox×2 → Grid×2+subgrid → MQ mobile-first/container queries/clamp → переменные+темы/transition+keyframes → финальный проект (каталог: grid-areas + auto-fit + CQ + clamp + тема + a11y). techs.js lessons: 22 — уже совпадал (счётчик главной честный). Ловушка: буллеты без пустой строки после абзаца схлопывались в параграф (markdown-lite) — python-фикс по всем файлам (пустая строка перед каждым `- `/`N. ` вне фенсов) + пересид. E2E: 22 урока на tech-странице, Lesson 1 of 22, callout'ы/буллеты/код-блоки рендерятся, Monaco со скелетом TODO. lint+build зелёные.
- 2026-09 (AI-промпт «в теме платформы»): модель приписывала несуществующие курсы/технологии — в сис-промпт (ai-prompt.js + дубль в api/ai.mjs) добавлен блок «Platform facts» (9 треков, курс HTML = 16 уроков, docs = Python, «не выдумывай»). E2E: «Есть Java?» → «Нет, ровно 9 треков… Java не в списке». Раунд 2 (негативный отзыв): модель выдавала дичь про историю/статистику — добавлены факты «Status and history» (запустились в 2026, early access, статистики нет, лидерборды/форум = demo-данные, «не выдумывай тысячи студентов») + правило на негатив (дружелюбно, без оправданий, упомянуть раннюю стадию). E2E: «Устаревшая, тысячи жалуются, сколько вам лет?» → «только запустились в 2026, ранний доступ, статистику не публикуем» (без выдумок). Ловушки: `\"` в промпте → lint no-useless-escape (типографские «кавычки»); E2E-«готово» = стабильность длины ответа 2с (typing гаснет с первым токеном).
- 2026-09 (AI Assistant, в working tree): статичный демо-чат в rail-карточке технологии заменён живым — модель **google/gemma-3-12b-it** через HuggingFace Inference API. Ловушки: (а) `gemma-2-9b-it` в каталоге Inference Providers больше НЕТ (NXDOMAIN legacy `api-inference.huggingface.co`; актуальный роутер — `router.huggingface.co/v1`); (б) fine-grained токен только с `repo.content.read` → «sufficient permissions to call Inference Providers» — нужен classic read; (в) модель пишет fence в строке с кодом и обрывается на max_tokens с незакрытым ``` → `fixMd()` в AiChat (fence-новая строка + автозакрытие нечётного числа ```). `src/lib/ai.js` (SSE-стриминг, X-Wait-For-Model, temp 0.4, max_tokens 900, сис-промпт = репетитор + трек студента + markdown-lite без таблиц/буллетов) + `src/components/AiChat.jsx` (стриминг в bubble, typing, авто-скролл, история 10, Enter/кнопка, «новый разговор», abort на unmount, ответы → MdContent). Чип модели динамический (`AI_MODEL_SHORT` из `src/lib/ai.js`). i18n: `ai.{thinking,send,clear,noKey,err*}` ×5. Без `VITE_HF_API_KEY` — режим «прогрев» (input disabled). (Ключ позже ушёл в `.env.local`/Vercel env — см. архитектуру выше.) **После изменения `.env*` dev перезапустить.** E2E: вопрос про замыкания → стриминг на русском + код-блок с Copy. Осталось: E2E второго/третьего сообщения (история), paid-tier при росте квоты.
- 2026-09 (Medium-новости, коммиты 653c17c/791df5e/e204dec/64eb554 + текущий): подключение Medium — RSS-теги **по технологиям платформы** через шлюз rss2json.com (у Medium нет CORS) → `src/lib/medium.js`. **Источник правды = TECHS** (тот же список, что «Дорожная карта»): `TECH_MEDIUM` мапит tech → тег Medium + regex-ключи, `activeFeeds()` из TECHS в рантайме → убрали технологию из TECHS = её лента не тянется. **Дневная ротация**: `isPublishedToday()` (только сегодня, pubDate как UTC → локальный день), `mediumDayKey()` + localStorage `syntax-medium-day` — при mount (initializer) и в интервале при 00:00: `clearSeenLinks()` + refresh; seen — `syntax-medium-seen`; поллинг 10 мин. Header-дропдаун — только новости (метка `t("home.tech.<id>")`). Клик → `NewsModal.jsx`: **ПОЛНАЯ статья** (Jina Reader, CORS, `target_selector=.postArticle-content` → markdown; `cleanArticleMarkdown`: мусор/аватар/**пейволл-строка «Not a Medium member» (обёртка `> [**…**](url)`)**/карточка sign-in/списки→«• »/нечётные ```-фенсы; **аватар автора вытаскивается из первого непустого ряда** (body начинается с \n — ловушка `idx===0`)), модалка **70vw** (колонка 860px), шапка: chip-источник + **byline (аватар-img, фолбэк Avatar-монограмма с hue от имени, автор, дата одной строкой)**; лейбл «Перевод» и футер-кнопки убраны; **sticky-крестик** (position:sticky top:16 в flex-панели, negative margin, opaque bg — закрывает и внизу статьи без скролла вверх); свёрнутый превью 700 зн. (MdContent prop `limit`/`blocks`) + «Learn more/Show less» (только если длиннее). **Перевод ПО БЛОКАМ** (`translateArticleBlocks`): только p/h2/h3/callout — **картинки/код через MT не проходят (причина «не грузится картинка»)**; партии по 6 (MyMemory 429 на бёрсты). **MyMemory-квота**: `de=frontend@syntax.dev` в URL — ~50K зн./день вместо 5K анонимных (причина «текст не переводит» — квота исчерпывалась на 1–2 статьях); флаг `quotaExhausted` при MYMEMORY WARNING — дальше запросы не шлём, фолбэк на оригинал; кэш на сессию. markdown-lite: ссылки `[t](url)`, картинки `![alt](url)`. i18n: `news.{loading,learnMore,showLess}` ×5; мёртвые ключи (read/original/published/byAuthor/translated, notifications.items/news) удалены. setState в модалке только в async-колбэках. Мобилка: bottom-sheet ≤900px; колокольчик ≤480 скрыт. Проверено puppeteer: 70vw, аватар в byline, RU-перевод после исчерпанной квоты (de-параметр), 1/1 картинка загружена ДО и ПОСЛЕ перевода, пейволл-строки нет, sticky-крестик виден на scrollTop=max (btnTopRel=17). **Доработка (текущий)**: sticky-крестик выше (top:12) + `flex:0 0 auto` (flex-колонка панели «сжимала» кнопку — сплющивание); **skeleton-лоадер** (ванильный CSS shimmer: hero-блок + строки, `@keyframes news-shimmer`) на время загрузки Jina Reader + инлайн-спиннер «Переводим…» на время перевода; инлайн-код `.md-inline` — фрейм (фон/бордер/моно, ванильный CSS — в lessons тоже); **`wrapCodeLines()`** в cleanArticleMarkdown: Jina ломает кодовые блоки в сырые строки (и разводит их пустыми строками!) — хэвистик CODE_START/CODE_CONT сворачивает последовательные «кодовые» строки в ```-блок → рендерится фреймом .code-block с Copy (ловушка: `/` без бэкслешиша в regex-литерале обрывает его — `/>` писать как `\/›`; пустая строка допустима внутри кодовой группы). MyMemory: и анонимные (5K), и de- (50K) квоты ~22ч на IP — флаг quotaExhausted даёт штатный фолбэк на оригинал (проверено). **Раунд «проверь перевод»**: (а) бейдж колокольчика горел ВЕЧНО — дропдаун показывал 5 из 8 новостей, а считался unread по всем; теперь в списке ВСЕ (max-height 72vh + скролл), E2E: прочитал 8/8 → бейдж гаснет, 0 красных. (б) перевод — цепочка провайдеров в translate.js: MyMemory (de=) → Google gtx (фолбэк) → оригинал; ловушки: MyMemory-квота приходит как HTTP 429 с JSON-WARNING в теле (читать тело ДО `!res.ok` throw, responseStatus 429 — не 200!), Google-429 на datacenter-IP — БЕЗ CORS (fetch-reject, не status) → счётчик googleFails (3 → googleBlocked на сессию). Сходимость: ~2 mymemory + ~8 google запросов на статью вместо 40+ шторма. Jina-мусор пополнен: 'Just now', '--'. На datacenter-IP оба провайдера могут лежать одновременно (MyMemory-квота ~22ч, Google 429) — фолбэк на оригинал штатный; с residential-IP работает хотя бы один. **Третий провайдер — MyMemory через Jina Reader** (`r.jina.ai/<mymemory-api-url>`): Jina ходит на API со СВОЕГО IP, поэтому переводит даже когда наша IP-квота MyMemory «лежит» на сутки (Google /m-страница не годится — JS-шелл); парсинг: JSON после «Markdown Content:». Плюс in-flight дедупликация translateText (StrictMode дублирует эффекты → без дедупа каждый блок шёлся 2 раза) и один retry на Jina-429 (20 RPM без ключа; 6 параллельных — ок, под нагрузкой бывают 429). E2E: статья на RU переводится целиком (заголовок + текст) за ~10–30 с, картинки целы; на datacenter-IP цепочка работает через jina-обход. **Ускорение перевода (фидбек «долго, хед не переводится»)**: (а) ПРОГРЕССИВНО — `translateArticleBlocks(blocks, lang, {priorityIdx, onBatch})`: батчи по 10, готовые блоки подменяются на лету (onBatch → `progress {done, texts}` в NewsModal, setState в async-колбэке), превью-блоки (до PREVIEW_LIMIT) — приоритетом: **видимая часть становится русской за ~3 с**, остаток фоном; (б) `lastGood` — последний работающий провайдер (mymemory/google/jina) первым в цепочке следующих запросов, без обхода мёртвых; (в) shimmer-прогресс-бар над статьёй + счётчик «🌐 Переводим… N / M» (`news.translatingProgress` ×5, `news.translating` удалён). «Хед не переводится» — ощущение: раньше UI обновлялся только ПОСЛЕ всей статьи. Замер: хед 2.8 с, вся статья (90 блоков) 11.9 с (было 30+). **Разнообразие (фидбэк «все новости по Python»)**: вместо «топ-8 самых свежих» (одна активная технология захватывала список) — ROUND-ROBIN: shuffle внутри каждой технологии, потом по одной статье с каждой техи (round 1), второй круг — до TOP_N=8; сет меняется при каждом refresh. **Цепочка RSS-шлюзов** (`fetchFeedItems`): rss2json (JSON, **free-дневной лимит ~200 — горел под тестами**, 429 "Daily rate limit exceeded, try again after 20 hours") → allorigins (XML, DOMParser) → corsproxy.io (XML; теперь 401 — API-ключ) → **Jina Reader на RSS-URL** (markdown-дамп: блоки `### [](url)` + дата RFC2822; title пустой → `slugToTitle()` — slug→заголовок). Сессийные gwFail-флаги (2 фейла → мёртвый на сессию). Ленты — ПООЧЕРЁДНО с зазором 1050мс (~1 req/с) + in-flight дедуп (newsPending, StrictMode). **TTL-кэш localStorage** (`syntax-medium-cache`, 2 ч): поллинг 10 мин не перечитывает сети иначе дневные квоты сгорят за часы; смена дня (refresh) — мимо кэша. **Баг**: `isPublishedToday` делал `.replace(" ","T")+"Z"` — RFC2822-даты (Jina/XML) превращались в Invalid Date и ВСЕ items вылетали; теперь сначала нативный `new Date(s)`. E2E: 8 новостей, 6 тех в дропдауне (HTML/React/Node/MongoDB/Python/PostgreSQL), при мёртвом rss2json (cooldown 20ч) всё живёт через Jina.
- 2026-09 (утечка HF-токена → прокси + переписанная история, коммиты f6b9c12/ecc5dd2/88e563a): токен HuggingFace лежал в закоммиченном `.env` (`VITE_HF_API_KEY` — публичное репо + VITE_* ещё и в бандле). GitHub **push protection** режал пуш ошибкой «push declined due to repository rule violations» (НЕ ruleset/branch protection — обе пусты; сообщение такое же у secret-push-protection). Лечение: (а) **Vercel serverless-прокси `api/ai.mjs`** (SSE passthrough к router.huggingface.co; токен — Vercel env `HF_API_KEY`, Production, БЕЗ VITE_-префикса); клиент `src/lib/ai.js` двухрежимный: dev-ключ из `.env.local` (gitignored `*.local`) → прямой HF, prod → `/api/ai`; `.env` в репо — только Supabase publishable-ключи (публичные по дизайну). (б) **Ротация токена** (старый revoke на HF). (в) **История переписана** `git filter-branch --tree-filter 'sed на .env'` origin/master..master: `git reset --soft HEAD~2` НЕ годился (токенный коммит был глубже, а HEAD~2 потеряет 24 коммита); после — `update-ref -d refs/original/...` + `reflog expire` + удалить `.git-rewrite`; токен только в НЕПУЩЕНОЙ части → push остался **fast-forward без force**; чистота проверки: `git rev-list origin/master..master | while read c; do git grep -q TOKEN $c; done`. (г) Ловушка Vercel: **кросс-папный import** из `../src` в serverless-функции → FUNCTION_INVOCATION_FAILED (даже на GET) → функция самодостаточная, сис-промпт продублирован в `api/ai.mjs` (синхронизировать с `src/lib/ai-prompt.js` при правках); диагностика: GET `/api/ai` → {ok, node, fetch, hasKey}. (д) Авторизация пуша с WSL: `git config --global credential.helper store` + `~/.git-credentials` с PAT (логин = GitHub-username, НЕ отображение; пароль для git не принимается). E2E на проде: 16 уроков, NEW-метки, AI-ассистент стримится через прокси, `hasKey: true`.
- 2026-09 (hero-мокап: печать кода ~10с, в working tree): фидбек «анимация показывает несколько строк и останавливается» — и правда: **Chrome недёргано теряет fill-состояние завершённых CSS-анимаций** (playState=finished, fill forwards, keyframes корректны — а computed-значение остаётся «до»): проявляется для окон end-time ~1.5–8.7с, воспроизведено на headless-shell И полном Chrome (headless и non-headless на :0) И даже на чистом HTML-репликаторе (без React); триггер-комбинация: `steps` + сверхкороткая длительность (0.05с) + layout-свойство (width/left), opacity-аналог тоже гибли в «мёртвом окне». **Лечение: печать теперь JS-типинг** в `HeroDemo` (MainView.jsx): rAF-цикл, позиция по `performance.now()` (time-based — любой сталл часов = допрыг до верного положения), `CODE_LINES` = данные {ch, node}, `CODE_TIMING` — кумулятивные тайминги (0.05с/символ, пауза 0.1с, старт 0.3 → вся печать ~10.4с); JS пишет `line.style.opacity`, `code.style.width = Nch`, курсор-`.hero-demo__caret` (left calc(34px + Nch) + visibility-окно 0.6с после строки, на последней — вечное мигание); по завершении — класс `hero-demo--done` на корне → `.hero-demo--done .hero-demo__pass` (pass-pop, вместо animation-delay 10.8s). CSS остался только базовый: мигание курсора (`code-cursor` infinite) и reduced-motion-блок (показывает всё сразу; JS-ветка reduce только вешает `--done`). Ловушки на пути: (а) в shorthand `animation: code-type steps(var(--ch), end) forwards;` без длительности → 0s → строка мгновенно «печаталась»; (б) курсор `::after` внутри `overflow:hidden` code обрезался клипом до конца строки → вынесен в отдельный absolutely-positioned span; (в) **`linear` + 0.01s + `forwards`** в последней анимации списка тоже терял fill (ещё один Chrome-квирк, ловится только чистыми HTML-репликами). Проверено E2E (headless + non-headless): прогресс t2/t6/t12 — посимвольная печать, курсор за печатной позицией, в финале все 8 строк + «3 tests passed».
- 2026-09 (переезд AI на Gemini 3.6 Flash, в working tree): по фидбеку «AI не работает» выяснилось: (а) в промпте не было даты — модель (cutoff прошлый) гадает «16 мая 2024»; фикс — строка `Current date: today is …` в `buildSystemPrompt()` (оба файла, дата на лету); (б) **HF 402: месячные кредиты Inference Providers сгорели** (dev и prod — один аккаунт). Решение: полный переезд на **Google Gemini**. Ключ AI Studio → `.env.local` `VITE_GEMINI_API_KEY` (HF-ключ удалён) + `GEMINI_API_KEY` в Vercel env (добавил пользователь). Ловушка: `gemini-2.5-flash` → **404 «no longer available to new users»** (API сам подсказал `gemini-3.6-flash`); это thinking-модель (`thoughtsTokenCount` ~370) → `maxOutputTokens` 2048. `api/ai.mjs` переписан под Gemini (самодостаточный, промпт-дубль с датой). E2E в UI: «какой сегодня день?» → «Среда, 2 сентября 2026», второй вопрос (история) → 16 уроков HTML, чип `gemini-3.6-flash`. Prod: коммит 82ca3c1 задеплоен, GET /api/ai → hasKey: true, E2E на syntax-sooty.vercel.app: два вопроса + история + чип gemini-3.6-flash.
- 2026-09 (сайдбар/виджеты/редактор, коммит deba971): (1) **Урок = TechAside** — в TAB_ASIDES добавлен `lesson: TechAside` (гостевой контент rail — daily challenge/promo — внутри урока не рендерится; techId для AI — из job). (2) **Прогресс под логотипом — реальный**: Sidebar получает `dbLessons`+`activeTech` от App; pct = выполненные (getCompleted, тот же источник, что Submit-гейт) ÷ уроки трека в БД; кольцо — inline `strokeDashoffset = 119.4*(1-pct/100)` (статичный `.streak.is-57` убран, transition 0.8s); текст «{n} of {m} lessons» (sidebar.progressTitle/progressCount/progressNoTrack ×5); рендерится `isAuthed && dbLessons` (loading → не мигает). (3) **AI-чип — реальная модель**: `AI_MODEL_DISPLAY = "Gemini Flash Lite"` в ai.js (чип), full id в title; заглушка «Syntax AI» убрана. (4) **Resources — живые ссылки**: `TECH_RESOURCES` в TechAside (2 ссылки на трек: MDN/web.dev/официальные доки React/Vue/Node/Mongo/Postgres/Python/JS) — `<a target="_blank" rel="noopener noreferrer">` с external-иконкой (CSS: .tech-aside__resource — cursor pointer, hover primary). (5) **Книга Flanagan**: promos.js `book: { cover: "flanagan", url: amazon dp/1098151219 }`; PromoCard: обложка CSS'ом (`.promo__cover` — чёрная, жёлтый «JavaScript», O'REILLY/7th/FLANAGAN, жёлтая планка снизу — референс оригинала), CTA = `<a>` на Amazon (target blank); i18n promo.book ×5 («JavaScript: The Definitive Guide» и локализации). (6) **Вёрстка редактора**: корень бага — фиксированные высоты (`.lesson-view__panel--editor .code-card--full { height: calc(100vh-300px); min-height:520px }` + wrapper `min-height:480px` + `overflow:hidden` карточки) → при низких окнах контент раздувал карточку, консоль/шапка обрезались/уезжали. Лечение: **flex-цепочка без фиксов**: `.lesson-view { height: calc(100vh - var(--topbar-h) - 48px); min-height: 560px }`, `.lesson-view__panel { flex:1; min-height:0; display:flex column }` (hidden-правило — `.lesson-view__panel.lesson-view__panel--hidden` с повышенной специфичностью, иначе display:flex перебивает display:none), `.lesson-view__material { flex:1; min-height:0; overflow-y:auto }` (материал скроллится ВНУТРИ), editor: `height:100%; min-height:0`, `.code-card__editor-wrapper { min-height: 0 }` (sandbox держит .code-card--full min-height 550). Result: head/tabs/консоль/превью ВСЕГДА в кадре при любом размере окна (проверено 720/600/812-mob), страница не скроллится. E2E: прогресс 0%→Submit→«1 of 16 lessons» (6%), мобильный урок — карточка в кадре, bottom-nav статичный (не фиксируется — не перекрывает).
- 2026-09 (гостевая приватность + редизайн, коммит 86a01c4): (1) **гостям закрыты уроки** — MainContent: `requireAuth()` (isAuthed из session) стоит в `openLesson`/`openDbLesson`; любой гостевой переход (Try a demo lesson, Continue на roadmap, клик по уроку на tech-странице, Daily Challenge у гостя) → AuthModal с ctx ("lesson"/"challenge"); таски/песочница остались открытыми. (2) **Блок Success Rate переработан**: кольцо 140px с ЧИСЛОМ ВНУТРИ (count-up `useCountUp` 94%) — hero композиции; sweep при загрузке (двойной rAF + transition 1.4s), после — «дышащее» свечение `.is-live` (ring-breathe, 1.5s задержка); градиентная дуга (svg linearGradient primary→accent-2), hover: scale 1.04 + ускоренное дыхание; подпись справа (label + successSub + delta в кольце); мобилка — колонкой по центру. Ловушка: `setRingLive(true)` синхронно в useEffect → react-hooks/set-state-in-effect → initial state из matchMedia. (3) **Отзыв ученика удалён** (home__quote секция + CSS; i18n-ключи quote.* не тронуты). (4) **Daily Challenge — динамический**: `src/lib/daily.js` (seeded RNG: FNV-1a + mulberry32 от локальной даты `YYYY-MM-DD` — у всех одно испытание на день, стабильно при визитах) из пула ТЕХ, у кого реально есть строки в `lessons`; `src/lib/lessonJob.js` (TASK_FILE + `lessonJobFor` — общий конструктор job: MainContent и DailyChallenge); `src/components/DailyChallenge.jsx` — shared-компонент (TasksAside + home-rail WidgetPanel): состояние загрузки (скелетон `.roadmap__skel`), fallback на статичное при пустой БД, реальный таймер до полуночи (`secondsToMidnight`, не фиксированные 14:22:05), chip технологии, guest Accept → AuthModal ctx challenge, authed → `#/lesson`. Ловушка: `dbLessons === null` (loading) → `daily = null` → `daily.lesson` crash весь tree — `isDaily` с гвардом `dbLessons !== null`. App → WidgetPanel: `dbLessons` + `onAuth`. i18n ×5: home.stats.{successSub,successDelta}, tasks.daily.lessonNote({tech}).
- 2026-09-02 (UX-аудит платформы (Playwright/puppeteer, prod) → исправления, коммит 4c92c0d): полный обход 10 вкладок + урок/Run/Submit + модалки + 1920/375. Найденное и починенное: **H1** bottom-nav 375px — подписи перекрывались → на ≤560px иконки-only (aria-label, chevron скрыт); **H2** мобильная таблица рейтинга обрезала ранг/XP («1…») → корень: базовый `padding: 24px` объявлен ПОЗЖЕ media-блока (при равной специфичности побеждает) → селектор с `.rankings__table-scroll` (выше специфичность), col-streak скрыт, rk-delta спрятан, col-rank/col-xp со своими ширинами; **H3** Submit на не тронутом TODO-скелете давал «accepted» → гейты: без изменений → только консоль-подсказка + revealLineInCenter к первому TODO, с TODO → amber-статус `editor-status--todos` «{n} TODOs left», `job.onComplete` (прогресс) — только при полном успехе; **H4** RU-контент при 5-язычном UI → чип «RU» (lessonView.courseLang) в шапке урока и в заголовке Lessons на tech-странице; **M1** новости: `**[link](url)**` → link без звёзд, «thumbnail showing» SEO-абзацы выкидываются, `_курсив_` снимаем, чип `news.originalNote {n}` при сбое перевода части блоков; **M2** Settings/Support — SOON-бейдж в заголовке; **M4** tasks/roadmap: трек «none» у гостя — roadmap честно показывает демо (не претендую, оставил как есть после аудита); **M5** «Start Challenge» у гостя → AuthModal с контекстным заголовком (signup.titleChallenge, AuthModal prop ctx, App openAuth(mode, ctx)); **M6** пустое превью после Run → оверлей-подсказка `.editor-preview-hint` (ловушка: `<style>`/`<script>` baseStyle считаются «текстом» — вычищать ДО проверки на пустоту); **M7** дропдаун уведомлений — «Mark all as read» (`markAllLinksSeen` в medium.js, бейдж гаснет); **L5** чип AI-модели → «Syntax AI · Flash» (title — полное имя); **L6** таймер challenge — подпись «Resets daily at midnight». Не тронуты (Low + риск конфликтов с параллельной сессией): M8 демо-статистика (50k/84.5k), L1 promo «Buy Now», L3 «Top 100». i18n: новые ключи ×5 (courseLang/originalNote/resetsDaily/titleChallenge…), i18n-аудит 168 ключей зелёный. E2E на проде: nav без перекрытий, 0 clipped-ячеек рейтинга, RU-чип, честный Submit, SOON на Settings.
- 2026-09-02 (AI-ускорение: flash-lite, коммиты 5e200e8/85e46e6): «модель долго думает» — замер: gemini-3.6-flash = thinking-модель, 447 мысленных токенов → 10с до ПЕРВОГО токена ответа (стрим после того мгновенный). Варианты: `thinkingBudget: 0` → 400 (не поддержано); **`thinkingConfig: { thinkingLevel: "low" }` → ~2с** (простые вопросы — thoughts: 0); `gemini-flash-lite-latest` → 0.8–1.5с, не думает, ответы на код/даты/платформу — ок. Но в проде поймано: **free-tier gemini-3.6-flash = 20 req/день** (429 RESOURCE_EXHAUSTED, quotaValue "20") — для платформы мало. Итог: **модель = gemini-flash-lite-latest** (обе копии), thinkingConfig убран (lite не думает). UI-замер: полный ответ за ~2с (было 10–25). Ловушка 429-тела: квота в `details[]` (quotaValue) — читать перед throw. Ещё 503 «high demand» бывает временным — retry помогает.
- 2026-09-02 (AI: дата + 402-квота, в working tree): «AI не знает какой день» — в сис-промпте НЕ БЫЛО текущей даты (cutoff gemma-3 — прошлые годы, гадает «16 мая 2024»). Фикс: `buildSystemPrompt()` подставляет `new Date().toLocaleDateString("en-GB", {weekday, year, month, day})` строкой «Current date: today is … NEVER answer dates from your training data» — **В ОБЕХ КОПИЯХ** (src/lib/ai-prompt.js — dev, api/ai.mjs — prod; дата берётся на стороне вызова: браузер/Vercel). Проверено: «Какой сегодня день?» → «Среда, 2 сентября 2026», «сколько дней до 1 января» → 97. Вторая причина «вчера было лучше» — **HF-кредиты**: Inference Providers съели месячные included credits (HTTP 402 «depleted your monthly included credits») — ассистент в UI падал generic-ошибкой. Теперь 402 → `AiError.code = "credits"` (ai.js) → i18n `ai.errCredits` ×5 (EN: "Daily AI quota is exhausted…") — дружелюбное сообщение вместо «Something went wrong». **Чтобы AI снова отвечал — на HF-аккаунте пополнить pre-paid credits или PRO-подписка (20× лимиты), либо ждать месячного сброса; dev-ключ (.env.local) и Vercel `HF_API_KEY` — тот же аккаунт, 402 бьёт и dev и prod.**
- 2026-09 (Roadmap переделан на реальные курсы — в working tree): фидбек «причесать дорожную карту, у нас уже есть уроки». Было: демо-модули i18n `techs.{id}.modules` (4 штуки, «Модуль X из 4 · %» из контента). Стало: **таймлайн из реальных уроков трека из БД** (`lessons` с tech = id трека, порядок id) + **настоящий прогресс** — `src/lib/progress.js` (localStorage `syntax-progress-<tech>`, отметку ставит успешный Submit через `job.onComplete`; уже был, но roadmap его не знал). Состояния: (а) `dbLessons === null` = грузится → скелетон (шиммер, `.roadmap__skel*`); (б) есть уроки → «Урок X из N · a из b · %», done-строки кликабельны (повторить, `.roadmap__card--review`), current — доминирующий, locked после «Up next»; (в) пусто → empty-state в `.docs-empty`-стиле «Курс {tech} в разработке» + CTA на HTML-курс. `openDbLesson` получил 3-й аргумент `backTab` (roadmap передаёт "roadmap" — Back ведёт на карту). App: `setDbLessons(rows || [])` — null остаётся только «грузится», сбой/пусто = []. i18n: `roadmap.{lessonStatus,doneOf,courseDone,currentHint,emptyTitle,emptyBody,emptyCta}` ×5, trackDesc без «4 модуля», мёртвый `statusLine` удалён. **Факт: в БД уже 39 уроков — html 16, css 22, javascript 1** (CSS-курс появился с другой стороны); `course/js/` — JS-курс пишется параллельно. Ловушка: eslint подхватывает `course/js/code/*.js` (скелеты со TODO) → `course/**` в globalIgnores (eslint.config.js). E2E: HTML (16 строк, «Lesson 1 of 16»), CSS (22), React (empty-state), прогресс (2 отмеченных → «Lesson 3 of 16 · 2 of 16 · 13%», строка 1 Completed кликабельна), клик по строке → #/lesson «From database», мобилка ок.
- 2026-09 (курс JavaScript — 42 урока, в working tree): «JavaScript с нуля (ES6+)» — 9 модулей от переменных к fetch. `course/js/` = 00-roadmap.md + 42 md-урока + `code/01..42.js`. Логика: основы/типы (01–07) → поток/функции/замыкания (08–15) → массивы (16–21) → объекты/JSON (22–27) → ошибки/отладка (28–29) → DOM+события (30–35) → storage/таймеры (36–37) → event loop/Promise/async/await/fetch (38–41) → финальный проект (42). Каждый урок — фиксированный шаблон (Цель/Теория/Пример/Частые ошибки/Практическое задание, TIP+WARN, 4–7 КБ); код-скелет = рабочая стартовая версия с `// TODO` (НЕ решение), без top-level await (раннер = обычный `<script>`; асинхронка через IIFE/`.then`). DOM-уроки (30–35, 42) — HTML-каркас в ```html-блоке материала (студент создаёт index.html рядом — превью живое, JS инжектится автоматически). Сидер `seed-js-course.mjs` (корень): IDEMPOTENT (удаляет ВСЕ tech='javascript' → вставляет 42, id `30000000-…`), встроенный QC (5 разделов, TIP/WARN, ```js, объём 4000–7000, без [ссылок](url)) — падает до БД при нарушении; `DRY=1` — только проверка. Засидено: 42 строки tech='javascript' (заменил собой старый тестовый JS-урок — «javascript 1» из прошлой хроники). AI-промпт: строка Courses теперь HTML 16 + CSS 22 + **JavaScript 42** (в ОБЕИХ копиях). E2E dev: 42 урока на tech-странице, урок 5 — материал (callouts) + Run → консоль `false true false true 10 0 default` (7 строк, ожидаемые значения), Task-таб с Monaco и TODO-скелетом, урок 33 — HTML-каркас в материале. Prod (syntax-sooty): 42 урока, урок 5 с 10 WARN-каллаутами, Continue → урок 1. `techs.js` уже имел `javascript: 42` (честный счётчик). Lint/build зелёные.
- 2026-09 (регистрация/авторизация — в working tree): фидбек «нужно сделать регистрацию/авторизацию». Было: SignupModal-демо (submit просто закрывал, isAuthed in-memory, reset на refresh). Стало: **Supabase Auth (email+password)** — `src/lib/auth.js` (signInWithPassword/signUp/signOut/readStoredSession/onAuthChange/syncProfile/mapAuthError). Сессия персистируется supabase-js в localStorage; App держит `session`, `isAuthed`, `userName`; модалка `AuthModal` (SignupModal.jsx удалён) с режимами signup/login, key=mode-remount, busy, ошибки ×5 языков, панель «Check your inbox» при signUp без сессии. Header: гость → Log in/Sign up free; юзер → монограмма (hue от имени) + меню (имя/email, Профиль[soon], Settings, Log out). syncProfile → upsert profiles {id,email,full_name} (колонки таблицы: id/email/full_name/**avatarurl** — без подчёркиваний; проверено PostgREST-ошибками). Ловушки: (а) onAuthStateChange возвращает объект, не функцию (unsub через data.subscription.unsubscribe — упало с «unsub is not a function»); (б) react-hooks/set-state-in-effect на sync-reset модалки → условный рендер + key; (в) **Supabase 429 over_email_send_rate_limit** — проект исчертал лимит писем, signup блокируется ДО создания юзера (login тем же email → invalid_credentials); лечится выключением «Confirm email» в dashboard (Authentication → Sign In/Providers → Email) или ожиданием окна — **нужно действие пользователя**; код оба пути обрабатывает (сессионный логин сразу / «проверьте почту»). **E2E после выключения «Confirm email» — весь цикл зелёный**: signup → модалка закрылась, аватар-монограмма («F», hue от имени), гостевой блок ушёл, токен `sb-*-auth-token` в localStorage; refresh → сессия жива; меню «Final User | email»; logout → гость; login снова → тот же юзер; неверный пароль → «Wrong email or password» (ловушка: regex `/valid credentials/` не ловил «Invalid **login** credentials» — «login» между словами; стало `/invalid (login )?credentials/i`). profiles-upsert пока 403 (нет RLS-политики для authenticated) — некритично (имя из user_metadata), но правильная политика: `CREATE POLICY "auth full access" ON profiles FOR ALL TO authenticated USING (auth.uid() = id) WITH CHECK (auth.uid() = id);`).
- **Auth «до конца» (2026-09-02)**: (а) **Profile-страница `#/profile`** (`src/components/views/ProfileView.jsx`): гость — CTA-карта (Sign up/Log in через `onAuth`); юзер — монограмма (hue от имени), имя, email, «Member since {date}» (created_at, формат по языку DATE_LOCALES), блок «Course progress» — строки по трекам с курсом в БД (лого, «a of b», бар-градиент primary→accent-2, лого кликабельно → tech-страница; static-мапа TRACK_LOGOS), «Completed: N», Open roadmap, Log out. Меню хедера: Profile — рабочее (onNavigate("profile")). MainContent: `case "profile"` + пропсы session/userName/onAuth/onLogout от App. (б) **Forgot password**: логин-режим AuthModal — ссылка «Forgot password?» (`signup__forgot`) → подформа (только email) → `resetPassword(email)` (resetPasswordForEmail, redirectTo = origin+pathname) → панель «Check your inbox» (успех показываем ВСЕГДА — Supabase шлёт письмо даже незарегистрированному email); back-ссылка в логин. Ловушка: recover может дать временный 400 сразу после signup того же email (email-рэйтлимит проекта) — маппинг ошибок штатный. (в) **Прогресс имёнспейсится по юзеру** (`src/lib/progress.js`): гость `syntax-progress-<tech>`, юзер `syntax-progress-<uid>:<tech>`; uid — `currentUid()` из auth.js (синхронно из сохранённой сессии; call-сайты не менялись). **Наследование**: getCompleted юзера с пустым своим бакетом читает гостевой; markComplete юзера сидирует свой бакет гостевым — прогресс, накопленный до входа, не теряется. i18n: `auth.{forgot,forgotTitle,forgotSubtitle,forgotSubmit,backToLogin,resetSent}` + блок `profile.*` ×5. CSS: `.profile*`, `.signup__forgot` (хвост style.css). Побочный фикс: i18n-дыры от параллельной сессии (editor.submitUnchanged/submitTodosLeft/previewEmpty, notifications.markAllRead ×5) дособраны, audit снова зелёный (кроме roadmap.module — false positive).
- 2026-09-02 (авторизация «до конца»): (а) Profile-страница #/profile (ProfileView.jsx) — гость: CTA; юзер: монограмма/имя/email/Member since + реальный прогресс по трекам с курсами в БД (HTML 16/CSS 22/JS 42, бары, лого → tech-страница) + Open roadmap + Log out; пункт меню Profile стал рабочим. (б) Forgot password: подформа в логин-режиме (email → resetPasswordForEmail → панель «Check your inbox»; Supabase шлёт письмо даже незарегистрированному email — успех всегда). (в) Прогресс имёнспейсится по uid (syntax-progress-<uid>:<tech>), гостевой бакет наследуется: чтение — fallback, первый маркер — сидинг; call-сайты не тронуты (currentUid() внутри progress.js). i18n auth.forgot*/profile.* ×5 (ловушка: массовая вставка в locales-extra по якорю errGeneric — 3 языка схлопнулись в один блок; переделал с уникальными якорями на язык + git checkout файла). E2E dev: гость-профиль → гостевой прогресс (1 of 16 на roadmap) → signup → профиль «Profile User | Member since 2 September 2026» с наследованным HTML 1 of 16 → меню→Profile → logout → forgot (UI: Reset your password, 1 инпут, «Send reset link») → POST /recover 200 → панель «We sent a password reset link». Мобилка (390px): профиль в один столбец, signup-CTA работает. Lint/build/i18n-аудит (0 missing) зелёные. **RLS на profiles выполнен пользователем (2026-09-02, проверено)**: upsert/select/update authenticated — 201/200/200, anon заблокирован, syncProfile в приложении на signup — POST 201 (строка в profiles реально создаётся). Нюанс: политика вышла PERMISSIVE — каждый авторизованный видит ВСЕ профили (count=3 у чужого юзера), не только свой. Функционально ок (id/email/full_name/avatarurl ≈ публичные данные профиля; для community/rankings даже полезно), но если захотим защищать чужие записи от UPDATE/DELETE — сплит: `CREATE POLICY ... FOR SELECT TO authenticated USING (true);` + `FOR INSERT/UPDATE/DELETE ... USING/WITH CHECK (auth.uid() = id);`.
- 2026-09 (курс React — 18 уроков + React-раннер, в working tree): «React с нуля» (React 18+) — 5 модулей: React/JSX/компоненты/пропсы (01–04) → условия/useState/события/списки+key (05–08) → useEffect (cleanup, данные, ловушки: цикл/deps/stale closure) + useMemo/useCallback (09–12) → правила хуков/кастомные хуки/useFetch/useRef/Context (13–16) → debounce/abort + финальный проект «Заметки» CRUD (17–18). `course/react/` = 00-roadmap.md + 18 md + code/01..18.jsx. Контракт: тот же (5 разделов, TIP+WARN, 4000–7000 зн), QC требует ```jsx; код = исполняемый `App.jsx`-скелет с // TODO (компонент **App** — `function App`/`const App`/`export default`), современный синтаксис (createRoot, хуки, без классов), воспроизводим в Vite/CodeSandbox без изменений. **React-раннер в CodeEditor**: `.jsx` → `buildReactDoc()`: код → `<script type="text/plain">` → Babel standalone (unpkg; ловушка: data-presets НЕ парсит JSON → `Babel.transform` с `runtime: "automatic"`) → blob-module → `import()` → createRoot; React 18.3.1 с esm.sh через import map (одна копия для студента и раннера); `__SyntaxMount` обязателен через `window.`; `__syntaxMounted`-флаг от двойного маунта; offset = строка маркера в ТРАНСФОРМИРОВАННОМ коде (blob-координаты). Монaco: .jsx → "jsx" (emmetJSX +jsx/tsx), buildJsBlocks исключает jsx из сырых script. Сидер `seed-react-course.mjs` (id 40000000-…, IDEMPOTENT, QC, DRY) — засидил 18 строк tech='react'. AI-промпт: + React (18) в обеих копиях. E2E dev: 18 уроков, Run скелетов (06/18) — mount без ошибок, **полный пример урока 6** (вставлен в Monaco через window.monaco) — mount + клик «добавить» в превью обновил список (useState/functional setState/key-списки работают), урок 10 — материал с 15 callout'ами. Lint/build зелёные.
