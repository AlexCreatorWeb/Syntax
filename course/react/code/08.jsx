import { useState } from "react";

// Урок 8. Списки и ключи (key). Напишите компонент App.

// TODO: ShoppingList — useState с массивом { id, name, price, qty },
//       .map() с key={id}, кнопки «+» (qty) и «Удалить» (filter),
//       итоговая сумма через reduce, добавление новых товаров
function ShoppingList() {
  return null;
}

export default function App() {
  return (
    <div>
      <h2>Списки и key</h2>
      <ShoppingList />
    </div>
  );
}
