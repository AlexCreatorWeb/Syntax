// Урок 21. Иммутабельность и копирование. Запустите (Run) — смотрите консоль.

// «Примитивы** «по** «значению»:
let a = 5, b = a; b = 6;
console.log(a); // 5

// «Массив** «ссылка»:
const orig = [1, 2, 3];
const ref = orig;
ref.push(4);
console.log(orig); // ??? (ссылка!)

// TODO 1: «поверхностная** «копия**: [...orig] — «измените** «вложенный** «объект** «в** «копии**, «посмотрите** «оригинал**
//         const a = [{ x: 1 }]; const c = [...a]; c[0].x = 99; console.log(a[0].x) // ???

// TODO 2: updateItem(arr, id, patch) — map + spread (без «мутации»)
//         updateItem([{id:1,name:"a"}], 1, { name: "A" }) → [{id:1,name:"A"}]

// TODO 3: addItem(arr, item) — [...arr, item]

// TODO 4: «сравните** [...a] «vs** structuredClone(a) — «измените** «вложенный**, «посмотрите** «оригинал**
// TODO 5 (бонус): «объясните** «в** «комментарии**, «почему** «иммутабельный** «стиль** «лучше** «мутации»
