// Урок 29. Отладка: console.* и DevTools. Запустите (Run) — смотрите консоль.

const users = [
  { id: 1, name: "a", age: 15 },
  { id: 2, name: "b", age: 25 },
];

// TODO 1: «все** «уровни**: console.log / info / warn / error
// TODO 2: console.table(users) — «таблица** «вместо** «сырого** «массива**
// TODO 3: console.group("отчёт") ... console.groupEnd() — «группировка** «логов**
// TODO 4: console.count("цикл") «в** «цикле** «3 «раза** — «счётчик**
// TODO 5: console.time("замер") / timeEnd — «замер** «времени** «операции**
// TODO 6 (бонус): «создайте** «ReferenceError — «прочитайте** «стектрейс** «в** «консоли**
