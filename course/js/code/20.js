// Урок 20. sort, slice, flat. Запустите (Run) — смотрите консоль.

const nums = [10, 9, 2, 40];

// TODO 1: «сортировка** «по** «возрастанию/убыванию» (компараторы a - b / b - a).
//         «Не** «забудьте** «копию** ([...nums]), «иначе** «оригинал** «изменится»
// TODO 2: sortBy(arr, key) — «объекты** «по** «ключу» (число: a[key] - b[key], строка: localeCompare)
// TODO 3: topN(arr, n) — «первые** n «после** «сортировки** (сортировка + slice)

// «Демо»:
console.log(nums.slice(1, 3));  // ??? (кусочек)
console.log(nums.slice(-2));    // ??? (последние два)
console.log([[1, 2], [3, 4]].flat()); // [1, 2, 3, 4]

// TODO 4: flattenDeep(arr) — flat(Infinity). flattenDeep([1, [2, [3, [4]]]]) → [1,2,3,4]
// TODO 5 (бонус): «объясните** «мульти-сортировку** «по** «двум** «ключам» (|| «в** «компараторе»)
