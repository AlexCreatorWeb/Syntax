# Урок 42. Финальный проект: мини-приложение «Задачи + Поиск»

## Цель
Собрать «мини-приложение** «(««задачи** «с** ««поиском**, ««фильтром** «и** ««сохранением** «(«вместе** «всё** «из** «курса**: «переменные/функции** (M1–2), «массивы/объекты** (M3–4), «DOM/события** (M5), «localStorage** (36), «debounce** (37), «async/fetch** «(опционально** «(M6**). «Результат** — ««работающее** «приложение** «в** «браузере**.

## Теория
### «Архитектура**: «состояние** «+** ««отрисовка** «+** ««события** «(«паттерн** «из** «урока** 35**
- **«состояние**: todos = [{ id, text, done }], «filter** ("all" / "active" / "done"), «query** («строка** «поиска**);
- **«отрисовка**: render(): ««фильтр** «(done** «+** «query**) «→** ««список** «(«очистить** «и** ««создать** «li** «для** ««каждого** «(createElement + textContent**);
- **«события**: «добавить** (input + Enter/«кнопка**), «toggle** (делегирование** «на** «списке**), «удалить**, ««смена** ««фильтра** («табы**), «поиск** (input + debounce 300ms**);
- **«персистентность**: save/load «(localStorage** «(урок** 36** «—» ««переживёт** «перезагрузку**;
- **«статистика**: ««счётчики** «(«всего** «/ ««активные** «/ ««готово**) «(«каждый** ««шаг** «в** «render**).

### ««Фильтр** «и** «поиск** «(«модуль** «3**: «filter** «(17** «+** «includes** «(4**
```js
function visibleTodos() {
  return todos.filter((t) => {
    const byStatus = filter === "all" || (filter === "active" ? !t.done : t.done);
    const byQuery = t.text.toLowerCase().includes(query.toLowerCase());
    return byStatus && byQuery;
  });
}
```
«Каждый** ««изменение** «состояния** «(todos/filter/query) «→** render(). ««Список** ««всегда** ««синхронизирован** «с** ««состоянием**.

### «Дебаунс** «поиска** «(37**): «input** ««срабатывает** «каждый** «символ** — «debounce** «300ms «для** ««обновления** «query** «и** «render** (не** ««каждый** «символ** «(««перерисовка** ««дороже**).

### «Опционально**: ««облачный** «синк** «(41**): «fetch «POST «на** «JSONPlaceholder «(««демо** «(«создание** «задачи** «на** ««сервере**) «—» ««показать** «async/await «в** ««приложении**.

### «Чек-лист** ««завершённости**
- ««добавление** «задачи** (input + Enter/«кнопка**);
- ««отметка** «done** (toggle** «(«делегирование**);
- ««удаление** «(кнопка** «×** «(делегирование**);
- ««фильтр** «(табы** «Все/Активные/Готово**);
- «поиск** (input + debounce** «(300ms**);
- ««счётчики** «(«всего** «/ ««активные** «/ ««готово** «в** ««шапке**);
- ««сохранение** «(localStorage** «—» ««переживёт** «перезагрузку**);
- (бонус) «fetch** «POST** «на** «JSONPlaceholder** «(«облачный** «синк** «(демо**).

TIP: ««начните** «с** ««минимума** (««добавление** «+** ««отрисовка** «+** ««состояние**), ««потом** ««добавляйте** ««фичи** «(«фильтр** «→** «поиск** «→** ««сохранение** «→** «fetch**). ««Каждый** ««шаг** — ««рабочее** ««состояние** «(«протестируйте** «после** «каждого** «(«не** ««всё** «сразу**). «Имя** ««функций** ««говорит** «о** ««назначении** «(addTodo, «toggleTodo, «setFilter** «—» ««отладка** ««легче** «(урок** 29**).

NOTE: «этот** «проект** «—» ««мост** «к** «React/Vue** («состояние** «→** ««отрисовка**, ««иммутабельные** «обновления**, «id** «ключи** «(35**). ««Освоив** «этот** ««паттерн** «на** «vanilla** «—» ««переход** «к** «фреймворку** ««короткий**.

## Пример
HTML (index.html):
```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Задачи</title>
  <style>
    .todo { display: flex; gap: 8px; align-items: center; padding: 8px; background: #eee; margin: 4px 0; border-radius: 6px; }
    .todo.done .txt { text-decoration: line-through; color: #888; }
    .todo .txt { flex: 1; }
    .tabs button.active { font-weight: bold; }
  </style>
</head>
<body>
  <h1>Задачи</h1>
  <p id="stats"></p>
  <div class="add">
    <input id="input" placeholder="новая задача">
    <button id="add">+</button>
  </div>
  <input id="search" placeholder="поиск">
  <div class="tabs">
    <button data-f="all" class="active">Все</button>
    <button data-f="active">Активные</button>
    <button data-f="done">Готово</button>
  </div>
  <ul id="list"></ul>
</body>
</html>
```
JS (index.js «—» ««скелет** «в** «задании** «с** «TODO** (состояние**, render, ««события**, ««фильтр** «+** «поиск** «+** localStorage**).

## Частые ошибки
WARN: ««состояние** «в** «DOM** («class** «done** «—» ««источник** «правды**) — ««разрыв** «после** «render** (35**); «состояние** «—» «массив**, «DOM** «—» ««зеркало**.
WARN: ««каждый** «символ** «поиска** «—» «render** «(««перерисовка** ««дорого**); debounce** «300ms «(37**).
WARN: ««забыли** «save «после** ««изменения** «todos** (36**) — ««перезагрузка** ««теряет** ««данные** «(«сохраняйте** ««каждый** ««шаг** «(добавление** «/ toggle** «/ «удаление**).
WARN: ««обработчик** «на** ««каждом** «li** «(создаётся** «в** «render) — «10К «→** 10К «обработчиков** «(««утечка** «памяти** «(«каждый** «render**); «делегирование** «на** «#list** «(34**).
WARN: ««всё** «сразу** (««пишете** «500 «строк** «и** ««отладываете** ««все**) — ««минимум** «сначала** («добавление** «+** «render**), ««потом** ««фичи** «(«каждый** ««шаг** «—» ««рабочее** «состояние**).

## Практическое задание
Создайте index.html «с** «каркасом** «и** index.js «с** ««скелетом** «(«в** «задании**). «По** «чек-листу**:
1. «Состояние** +** «отрисовка**: todos «массив** «объектов** «({ id, text, done }), render(): «очистить** «#list** «и** «создать** «li** «(dataset.id, «класс** «done** «по** «состоянию**).
2. «Добавление**: input + Enter/«клик** «#add** «→** «новый** «todo** (id: nextId++) «→** render.
3. «Делегирование**: #list click → toggle (map «+** done: !t.done) / del (filter) → render.
4. «Фильтр**: «табы** «Все/Активные/Готово** «(dataset.f**) «→** «состояние** «filter** «→** ««фильтр** «списка** «в** «render**.
5. «Поиск**: #search «input** «→** debounce 300ms «(37** «→** «query** «→** ««фильтр** «(text.toLowerCase().includes** «(4** «→** render.
6. «Статистика**: #stats — ««всего** «N · «активные** «K · «готово** «M «(«каждый** ««шаг** «в** «render**).
7. «Сохранение**: load() «при** «старте**, save(todos) «при** ««каждом** «изменении** «(36**) — ««перезагрузка** ««не** ««теряет** «данные**.
8. Бонус: fetch POST «на** «JSONPlaceholder «(41** «—» ««облачный** «синк** «(демо** «(«отправка** ««созданной** «задачи** «на** ««сервер** «(console.log «id**).
